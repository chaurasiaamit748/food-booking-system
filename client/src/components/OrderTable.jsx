import StatusBadge from './StatusBadge';
export default function OrderTable({ orders, onStatus }) { return <div className="table-wrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Destination</th><th>Assigned to</th><th>Status</th><th>Updated</th></tr></thead><tbody>{orders.map((order) => <tr key={order._id}><td><strong>{order.orderId}</strong><small>{order.packageDescription}</small></td><td>{order.customerName}<small>{order.customerPhone}</small></td><td>{order.deliveryAddress}</td><td>{order.assignedTo?.name || 'Unassigned'}</td><td>{onStatus ? <select value={order.status} onChange={(event) => onStatus(order, event.target.value)}><option>{order.status}</option>{order.status === 'Assigned' && <option>Picked Up</option>}{order.status === 'Picked Up' && <option>Out for Delivery</option>}{order.status === 'Out for Delivery' && <option>Delivered</option>}</select> : <StatusBadge status={order.status} />}</td><td>{new Date(order.updatedAt).toLocaleDateString()}</td></tr>)}</tbody></table>{!orders.length && <div className="empty">No orders match this view.</div>}</div>; }
